# Image Downloader MCP Server Configuration

# Default download directory
DEFAULT_SAVE_PATH=./downloads

# Default filename pattern (optional, if not set, will use original filename)
# You can use placeholders like {timestamp}, {index}, etc.
# DEFAULT_FILENAME=image_{timestamp}

# Default image format (original, jpeg, png, webp)
DEFAULT_FORMAT=original

# Default compression setting (true/false)
DEFAULT_COMPRESS=false

# Default maximum width in pixels (optional)
# DEFAULT_MAX_WIDTH=1920

# Default maximum height in pixels (optional)
# DEFAULT_MAX_HEIGHT=1080

# Default concurrency for batch downloads (1-10)
DEFAULT_CONCURRENCY=3

# API Keys (if needed for future features)
# OPENAI_API_KEY=your_openai_api_key_here
# ANTHROPIC_API_KEY=your_anthropic_api_key_here